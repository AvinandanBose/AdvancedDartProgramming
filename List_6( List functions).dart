void main(List<String> args) {
  List<int> myList = [1, 2, 3];
  List<String> Lst = ['Avi'];

  print(myList.first); //first element of List
  print(myList.last); //last element of List
  print(myList.isEmpty); //if empty then returns true
  print(myList.isNotEmpty); //if not empty then returns true
  print(myList.reversed); // reverse the list
  print(Lst.single); //If single element is present in the list then return
  //otherwise error will return
}
