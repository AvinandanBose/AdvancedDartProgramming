void main(List<String> args) {
  List l = [1, 2, 3];
  l.addAll([12, 13, 15]); //note the values must be in list format
  print(l);
}

//The List.addAll() function accepts multiple values separated 
//by a comma and appends these to the List.
